//alt shift n to make new class, interface, etc.
public interface Drawable {
	//blueprint and ready to use
	//all variable in interface is final
	
//	int a = 10; //a is final. cannot change anymore. even method is abstract, must inherit.
	void draw(); //cannot have body
//	default void test(){
//		
//	}
}
